import { HouseFilterPipe } from './house-filter.pipe';

describe('HouseFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HouseFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
